name: testfluttereval
description: Example project for flutter_eval
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dart_eval: ^0.6.0
  flutter_eval: ^0.6.0
  cloud_firestore: ^4.9.0
  freezed_annotation: ^2.4.1
  freezed: ^2.3.2
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  after_layout: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/program.evc
